import SwiftUI
import SharedKit

struct ContentView: View {
    var body: some View {
        // TODO: 앱 UI 진입점을 여기에 연결합니다.
        // `SharedKit` 은 :shared 모듈이 만들어 주는 Kotlin/Native 프레임워크입니다.
        Color.clear
    }
}
