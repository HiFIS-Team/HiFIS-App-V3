package app.hifis.shared.search

/**
 * 헤더 검색이 쓰는 문구 — **두 플랫폼이 같은 말을 해야 한다**
 *
 * 화면마다 새로 적으면 한쪽만 고쳐지고 갈린다 (`AiPrompt` · `Notice.DATE_PATTERN` 과 같은 자리).
 *
 * **무엇을 뒤지는지 자리 이름을 늘어놓는다.** `검색` 한 마디만 두면 이 칸이
 * 지금 화면만 뒤지는지 앱 전체를 뒤지는지 알 수가 없다.
 *
 * 아직 **뒤질 것이 없다** — 서버도 색인도 안 붙였다. 지금은 빈 상태만 뜬다.
 */
object AppSearch {
    /** 입력칸이 비었을 때 보이는 글자 */
    const val PLACEHOLDER = "사람·공지·일정·문서·메시지 검색..."

    /** 아직 아무것도 안 친 상태에서 아래에 뜨는 한 줄 */
    const val EMPTY = "원하는 항목을 검색해보세요."
}
