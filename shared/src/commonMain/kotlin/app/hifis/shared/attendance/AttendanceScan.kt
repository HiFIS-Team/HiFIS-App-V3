package app.hifis.shared.attendance

/**
 * 출퇴근 스캔 화면의 문구 — 두 플랫폼이 **같은 말**을 한다
 *
 * 매장 카운터에 붙은 종이 QR 을 폰으로 읽는 자리다. V2 가 카운터 스캐너를
 * 걷어내며 만든 화면이라 문구도 거기서 그대로 가져왔다.
 *
 * **읽는 것은 아직 안 붙였다.** 서버가 없어서 읽어도 보낼 곳이 없다 —
 * 그래서 `찍는 중…` 같은 중간 문구도 아직 없다. 서버가 붙을 때 같이 온다.
 */
object AttendanceScan {
    /** 카메라 위 한 줄 — 조준 규격 아래에 선다 */
    const val GUIDE = "카운터에 붙은 QR 을 비춰주세요"

    /** 카메라를 못 열었다 — 권한을 막았거나 (시뮬레이터처럼) 카메라가 없다 */
    const val NO_CAMERA = "카메라를 열 수 없어요"

    /** 그 아래 한 줄 — 권한이 원인일 때 갈 길을 알려 준다 */
    const val NO_CAMERA_HINT = "설정에서 HiFIS 의 카메라 권한을 켜주세요."
}
